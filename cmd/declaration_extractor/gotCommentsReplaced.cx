package test_2

var global string

  

                         

  

type object struct {
	name   string            
	number int
}

const (
	Spring str = iota
	Summer
	Autumn
	Winter
)

func add(obj *object, name string, number int) {
	obj.name = name
	obj.number = number
}

func main() {

	var counter int = 10

	for i := 0; i < counter; i++ {
		fmt.Print(counter)
	}

	var instance obj

	add(instance,                         , 4)

	                     

	   

}
