package test_2

import (
	"fmt"
)

var global string

/*

Another multiline comment

*/

type object struct {
	name   string // var name
	number int
}

const (
	Spring string = iota
	Summer
	Autumn
	Winter
)

func add(obj *object, name string, number int) {
	obj.name = name
	obj.number = number
}

func main() {

	var counter int = 10

	for i := 0; i < counter; i++ {
		fmt.Print(counter)
	}

	var instance obj

	add(instance, "// single line comment", 4)

	/*func commented () {

	}*/

}
