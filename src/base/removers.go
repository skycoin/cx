package base

import (
	
)

// let's make these methods check if they are actually empty
// or not
// if, for example, we remove a function

// wait, how are we going to remove them?
// for maps, we'd use delete

func (mod *CXModule) RemoveFunction (fnName string) {
	
}

func (fn *CXFunction) RemoveExpression (line int) {
	
}
