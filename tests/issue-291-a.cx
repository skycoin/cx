package main

type too struct {
}

func (a *too) foo() (b bool) {
	b = true
	printf("foo\n")
}

func main() {
	var t too
	var cond bool
	cond = t.foo()
	for cond == true {
		cond = t.foo()
		printf("true\n")
	}
}
