package main

import "os"
import "cx"

func main () {
	var b bool = true
	var i i32 = 11

	if (assert(i, 11, "") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}

	if b {
		i = 17
	}

	if (assert(i, 17, "") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
}
