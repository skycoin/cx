package main
import "os"
import "cx"
func main () {
	var foo [1]i32

	var bar f32

	foo[0] = 3

	num := foo[0]

	if (assert(bar, 0.0, "") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
	if (assert(foo[0], 3, "") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
}
