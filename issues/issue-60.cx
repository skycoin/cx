package main
import "test_60a"
import "os"
import "cx"
func main ()(){
	printf("test_60a.i : %d\n", i)
	if (assert(false, true, "this should not compile") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
}
