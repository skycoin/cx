package main

import "issue50a"
import "os"
import "cx"

var test i32 = 44
func main()() {

	if (assert(issue50a.test, 666, "internal error") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
	if (assert(test, 44, "internal error") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
}
