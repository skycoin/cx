package main

import "cx"
import "os"

type issue30Type struct {
	ai [5]i32
}

func main()(){
	var too issue30Type
	too.ai[3 + 0] = 444
	if (assert(too.ai[3], 444, "") == false) {
		os.Exit(cx.INTERNAL_ERROR)
	}
}
