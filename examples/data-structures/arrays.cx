// Main Package 
// This package will explain us, how to work with the arrays 
package main

// main function 
func main () {
// initilization of an array variable of type i32
	var foo [5]i32
// assign the values to the array by index 
	foo[0] = 10
	foo[1] = 20
	foo[2] = 30
	foo[3] = 40
	foo[4] = 50

// print the values to the array by for loop 
	var c i32
	for c = 0; c < 5; c++ {
		i32.print(foo[c])
	}
// print the length of the array 
	i32.print(len(foo))
}
