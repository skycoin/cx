package main

func basicIf (num i32) (num i32) {
	pred := gtI32(num, 0)
	goTo(pred, 1, 3)
	printStr("Greater than 0")
	goTo(true, 10, 0)
	printStr("Less than 0")
}

func main () (out i32) {
	basicIf(5)
}
