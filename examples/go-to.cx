package main

func basicIf (num i32) (num i32) {
	pred := i32.gt(num, 0)
	goTo(pred, 1, 3)
	str.print("Greater than 0")
	goTo(true, 10, 0)
	str.print("Less than 0")
}

func main () (out i32) {
	basicIf(5)
}
