package main

func main () (out i32) {
	// This won't be evaluated
	//out := i32.div(5, 0) // This won't either

	out := i32.div(10, 5) // The program will return 2

	/*
        Comment block
	out := f32.sub(3.33, 1.11)
	out := f32.sub(3.33, 1.11)
	out := f32.sub(3.33, 1.11)
	out := f32.sub(3.33, 1.11)
        */
}
